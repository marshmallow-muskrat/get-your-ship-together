# Level art

Drop the multi-panel concept-art sheet here as `acts-composite.png`.

Panels are expected side by side, one per act, all the same width. The
prototype crops the panel it needs at draw time (`ACT_COUNT` in
`src/proto2d.ts`), so the sheet needs no splitting.
